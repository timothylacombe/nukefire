#nop This should run to start your sessions 'tt++ nuke.tin'

#class startup kill;
#class startup open;

#read common/initializer.tin
#read config.tin

#split 0 1;

connect ${charMain};

#gts
#if {numParty >= 2} {
	connect ${charTwo};
	}
	
#gts
#if {numParty >= 3} {
	connect #{charThree};
	};
#gts
#if {numParty >= 4} {
	connect ${charFour};
	};
#nuke:${charMain}


#class startup close;